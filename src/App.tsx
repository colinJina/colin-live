import './App.css'
import { Button } from 'antd';
import { useDrawer } from './provider/drawer-provider';
const App = () => {
  // const { openDrawer, showMessage } = useDrawer();

  return (
    <div className="App">
      {/* <Button type="primary"  onClick={
      showMessage
    }>Button</Button> */}
      {/* <span className='text-3xl font-bold underline'>1111safasf</span> */}
    </div>
  )

};

export default App
